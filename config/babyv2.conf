# Enable the USB HID transport for connection
CONFIG_ZMK_USB=y

# Set USB as the default output profile.
# ZMK uses profiles (0, 1, 2, etc.) for different connection types.
# Profile 0 is usually USB when enabled.
CONFIG_ZMK_BLE=n
CONFIG_ZMK_Default_OUTPUT=usb